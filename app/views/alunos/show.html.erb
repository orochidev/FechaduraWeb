<div class="row">
    <div class="col-md-3">
      <%= render 'pessoas/card_box', pessoa: @aluno %>
    </div>

    <div class="col-md-9">
      <div class="box box-primary">
        <div class="box-header with-border">
          <h3 class="box-title">Credenciais cadastradas deste aluno</h3>
        </div>

        <div class="box-body box-profile">
          <%= render 'pessoas/credenciais_table', pessoa: @aluno  %>
        </div>
        <div class="box-footer">
          <%= link_to "Adicionar novo RFID", new_aluno_rfid_path(@aluno), class: "btn btn-primary" %>
        </div>
      </div>

    </div>
  </div>
