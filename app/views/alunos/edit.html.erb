<% content_for :titulo do %>
Editar aluno <%= @aluno.to_s %>
<% end %>

<div class="box box-primary">
  <div class="box-header with-border">
    <h3 class="box-title">Editar aluno</h3>
  </div>
    <div class="box-body">
      <%= render 'form', aluno: @aluno %>
</div>

</div>
