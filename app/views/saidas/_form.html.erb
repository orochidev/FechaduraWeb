<%= form_with(model: saida, local: true) do |form| %>
  <% if saida.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saida.errors.count, "error") %> prohibited this saida from being saved:</h2>

      <ul>
      <% saida.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
